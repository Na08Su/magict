<div class="row">
  <div class="col m6 offset-m3 center">
    <h5>パスワードを忘れてしまいましたか？</h5>

    <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do  |f| %>
      <%= render 'shared/devisemes' %>

      <div class ="input-field">
        <%= f.email_field :email, id: "email", placeholder: "Eメール", autofocus: true %>
      </div>

      <button class ="btn waves-effect waves-light" type = "submit" name="action">send me reset pass instructions</button>
    <% end %>

  </div>
</div>
